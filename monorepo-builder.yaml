parameters:
    merge_sections:
        - 'require'
        - 'require-dev'
        - 'autoload'
        - 'autoload-dev'
        - 'repositories'

    package_directories:
        - 'src'

    package_alias_format: '<major>.<minor>.x-dev'

    # for "merge" command
#    data_to_append:
#        require-dev:

    # for "split" command
    directories_to_repositories:
